// Copy this file to config.js and fill in your own keys.
// config.js is listed in .gitignore, so it will never be committed.

export const NPS_API_KEY = "YOUR_NPS_API_KEY_HERE";

// Used starting in Phase 6 for the interactive map.
export const MAPBOX_TOKEN = "YOUR_MAPBOX_ACCESS_TOKEN_HERE";
